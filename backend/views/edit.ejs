<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Promotion - Promotions Manager</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h2>Edit Promotion</h2>
        <form action="/admin/update/<%= promotion.id %>" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="title_ru">Title (Russian):</label>
                <input type="text" id="title_ru" name="title_ru" value="<%= promotion.title_ru %>" required>
            </div>
            <div class="form-group">
                <label for="description_ru">Description (Russian):</label>
                <textarea id="description_ru" name="description_ru"><%= promotion.description_ru %></textarea>
            </div>

            <div class="form-group">
                <label for="title_en">Title (English):</label>
                <input type="text" id="title_en" name="title_en" value="<%= promotion.title_en %>">
            </div>
            <div class="form-group">
                <label for="description_en">Description (English):</label>
                <textarea id="description_en" name="description_en"><%= promotion.description_en %></textarea>
            </div>

            <div class="form-group">
                <label for="title_ka">Title (Georgian):</label>
                <input type="text" id="title_ka" name="title_ka" value="<%= promotion.title_ka %>">
            </div>
            <div class="form-group">
                <label for="description_ka">Description (Georgian):</label>
                <textarea id="description_ka" name="description_ka"><%= promotion.description_ka %></textarea>
            </div>
            <div class="form-group">
                <label>Current Image:</label>
                <% if (promotion.image_src) { %>
                    <img src="<%= promotion.image_src %>" alt="Current Image" class="promo-img-thumb">
                    <p>Current Path: <%= promotion.image_src %></p>
                <% } else { %>
                    <p>No image set.</p>
                <% } %>
            </div>
            <div class="form-group">
                <label for="image">Upload New Image (SVG or PNG):</label>
                <input type="file" id="image" name="image" accept=".svg, .png">
            </div>
            <div class="form-group">
                <label for="image_src_url_new">Or New Image Source (URL):</label>
                <input type="text" id="image_src_url_new" name="image_src_url_new" value="">
                <input type="hidden" name="image_src_url_existing" value="<%= promotion.image_src || '' %>">
            </div>
            <div class="form-group checkbox-group">
                <input type="checkbox" id="is_active" name="is_active" <%= promotion.is_active ? 'checked' : '' %>>
                <label for="is_active">Is Active</label>
            </div>
            <button type="submit">Update Promotion</button>
            <a href="/admin" class="button cancel-button">Cancel</a>
        </form>
    </div>
</body>
</html>
